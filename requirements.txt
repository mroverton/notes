# Pinned with lower bounds chosen for Python 3.14 wheel availability.
# (For a real project you'd pin exact versions in a lockfile; kept light here.)

# Web framework + ASGI server
fastapi>=0.115
uvicorn[standard]>=0.34
# Required for the OAuth2 login form (parses multipart/form-encoded request bodies)
python-multipart>=0.0.18

# Database: SQLAlchemy 2.0 ORM + the modern psycopg 3 driver
SQLAlchemy>=2.0.36
psycopg[binary]>=3.2.10

# Settings loaded from environment variables / .env
pydantic-settings>=2.7

# Auth: password hashing (bcrypt) + JWT
bcrypt>=4.2
PyJWT>=2.10

# Testing
pytest>=8.3
httpx>=0.28
